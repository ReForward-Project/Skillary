# Skillary 프로젝트 실행 방법

> **작성일**: 2026-01-14  
> **프로젝트 경로**: `/Users/hyerhin/Desktop/hyerhin/Study/Skillary`

## 📋 사전 준비사항

1. **Java 21** 설치 확인
2. **Node.js** (v18 이상) 설치 확인
3. **MySQL 8.0** 설치 또는 Docker 설치
4. **Gradle** (프로젝트에 Gradle Wrapper 포함)

## 🚀 실행 방법 (터미널 명령어)

### 방법 1: 로컬 개발 환경에서 실행 (권장)

#### 1단계: MySQL 데이터베이스 실행

**옵션 A: Docker로 MySQL 실행**
```bash
# 프로젝트 루트 디렉토리에서 실행
cd /Users/hyerhin/Desktop/hyerhin/Study/Skillary

# MySQL만 Docker로 실행
docker run -d \
  --name mysql-skillary \
  -e MYSQL_ROOT_PASSWORD=root1234 \
  -e MYSQL_DATABASE=testdb \
  -e MYSQL_USER=test \
  -e MYSQL_PASSWORD=1234 \
  -p 3306:3306 \
  mysql:8.0

# 데이터베이스 초기화 (스키마 및 데이터)
docker exec -i mysql-skillary mysql -uroot -proot1234 testdb < mysql-skillary-db/init/01-schema.sql
docker exec -i mysql-skillary mysql -uroot -proot1234 testdb < mysql-skillary-db/init/02-data.sql
```

**옵션 B: 로컬 MySQL 사용**
```bash
# MySQL이 로컬에 설치되어 있다면
mysql -u root -p < mysql-skillary-db/init/01-schema.sql
mysql -u root -p < mysql-skillary-db/init/02-data.sql
```

#### 2단계: Spring Boot 백엔드 실행

**터미널 1번에서 실행:**
```bash
# 프로젝트 루트 디렉토리로 이동
cd /Users/hyerhin/Desktop/hyerhin/Study/Skillary

# 백엔드 디렉토리로 이동
cd spring-skillary-back

# Gradle로 실행 (로컬 프로필 사용)
./gradlew bootRun --args='--spring.profiles.active=local'

# 백엔드가 정상 실행되면 다음과 같은 메시지가 표시됩니다:
# "Started SpringSkillaryBackApplication in X.XXX seconds"
```

**또는 IDE에서 실행:**
- `SpringSkillaryBackApplication.java` 파일을 열고 실행
- 실행 설정에서 Active profiles: `local` 설정

**백엔드 실행 확인:**
- 백엔드가 정상 실행되면 `http://localhost:8080` 에서 접근 가능
- API 엔드포인트: `http://localhost:8080/api/auth/register`

#### 3단계: Next.js 프론트엔드 실행

**터미널 2번에서 실행 (새 터미널 창):**
```bash
# 프로젝트 루트 디렉토리로 이동
cd /Users/hyerhin/Desktop/hyerhin/Study/Skillary

# 프론트엔드 디렉토리로 이동
cd next-skillary-front

# 의존성 설치 (최초 1회만 실행)
npm install

# 개발 서버 실행
npm run dev

# 프론트엔드가 정상 실행되면 다음과 같은 메시지가 표시됩니다:
# "Ready in X.XXs"
# "Local: http://localhost:3000"
```

**프론트엔드 실행 확인:**
- 프론트엔드가 정상 실행되면 `http://localhost:3000` 에서 접근 가능
- 회원가입 페이지: `http://localhost:3000/auth/register`

### 방법 2: Docker Compose로 전체 실행

```bash
# 프로젝트 루트 디렉토리에서 실행
cd /Users/hyerhin/Desktop/hyerhin/Study/Skillary

# 전체 서비스 실행 (DB, 백엔드, 프론트엔드, Nginx)
docker-compose up -d

# 로그 확인
docker-compose logs -f

# 서비스 중지
docker-compose down
```

## 🔍 실행 확인 체크리스트

### 백엔드 확인
- [ ] `http://localhost:8080/api/auth/send-confirm` (POST) - 이메일 인증 코드 발송
- [ ] `http://localhost:8080/api/auth/register` (POST) - 회원가입

### 프론트엔드 확인
- [ ] `http://localhost:3000/auth/register` - 회원가입 페이지 접근
- [ ] 이메일 입력 후 "전송" 버튼 클릭
- [ ] 인증 코드 입력 및 확인
- [ ] 닉네임, 비밀번호 입력
- [ ] 회원가입 완료

## 🧪 테스트 시나리오

### 회원가입 기능 테스트

1. **이메일 완전 입력 검증**
   - 이메일을 `test@gmail`까지만 입력하고 전송 버튼 클릭 → 에러 발생해야 함
   - 이메일을 `test@gmail.com`까지 완전히 입력하고 전송 버튼 클릭 → 인증 코드 발송됨

2. **이메일 중복 체크**
   - 이미 가입된 이메일로 회원가입 시도 → "이미 사용 중인 이메일입니다" 에러

3. **닉네임 공백 허용**
   - 닉네임 앞뒤에 공백 포함하여 입력 → 공백이 포함된 채로 저장됨

4. **비밀번호 패턴 검증**
   - 영문, 숫자, 특수문자만 허용
   - 한글 등 다른 문자 입력 시 → "비밀번호는 영문, 숫자, 특수문자만 사용할 수 있습니다" 에러

5. **닉네임 중복 체크**
   - 이미 사용 중인 닉네임으로 회원가입 시도 → "이미 사용 중인 닉네임입니다" 에러

## 🛠️ 문제 해결

### 포트 충돌
- 백엔드 포트 8080이 사용 중인 경우: `application-local.yml`에서 포트 변경
- 프론트엔드 포트 3000이 사용 중인 경우: `next.config.mjs`에서 포트 변경

### 데이터베이스 연결 오류
- MySQL이 실행 중인지 확인: `docker ps` 또는 `mysql -u root -p`
- 데이터베이스 이름, 사용자명, 비밀번호 확인: `application-local.yml`

### 이메일 발송 오류
- 환경 변수 설정 확인 (SPRING_MAIL_HOST, SPRING_MAIL_USERNAME 등)
- 개발 환경에서는 실제 이메일 발송 대신 콘솔 로그로 확인 가능

## 📝 참고사항

- 백엔드와 프론트엔드는 **별도의 터미널**에서 실행해야 합니다
- 데이터베이스는 먼저 실행되어 있어야 합니다
- 환경 변수가 필요한 경우 `.env.dev` 파일을 참고하세요
- 개발 중 코드 변경 시 자동 재시작됩니다 (Spring Boot DevTools, Next.js Fast Refresh)

## 🎯 빠른 실행 방법

### 스크립트 사용 (가장 간단)

```bash
# 프로젝트 루트 디렉토리에서
./실행하기.sh
```

스크립트가 실행 안내를 표시합니다.

### 수동 실행 명령어 (복사해서 사용)

### 전체 실행 (3개 터미널 필요)

**터미널 1 - MySQL 확인:**
```bash
docker ps | grep mysql
# MySQL이 실행 중이어야 합니다
```

**터미널 2 - 백엔드 실행:**
```bash
cd /Users/hyerhin/Desktop/hyerhin/Study/Skillary/spring-skillary-back && ./gradlew bootRun --args='--spring.profiles.active=local'
```

**터미널 3 - 프론트엔드 실행:**
```bash
cd /Users/hyerhin/Desktop/hyerhin/Study/Skillary/next-skillary-front && npm run dev
```

### 서비스 중지

**백엔드 중지:**
- 터미널에서 `Ctrl + C` 누르기

**프론트엔드 중지:**
- 터미널에서 `Ctrl + C` 누르기

**MySQL 중지 (Docker 사용 시):**
```bash
docker stop spring-skillary-back-db-1
```

## ✅ 실행 확인

서비스가 정상 실행되었는지 확인:

```bash
# 프론트엔드 확인
curl http://localhost:3000

# 백엔드 확인
curl -X POST http://localhost:8080/api/auth/send-confirm \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com"}'
```

브라우저에서 접속:
- 회원가입 페이지: http://localhost:3000/auth/register
- 로그인 페이지: http://localhost:3000/auth/login

## 📌 중요 참고사항

1. **이메일 설정**: 개발 환경에서는 실제 이메일 발송이 필요할 수 있습니다. `application-local.yml`에서 이메일 설정을 확인하세요.

2. **데이터베이스**: MySQL이 실행 중이어야 합니다. Docker로 실행 중인 MySQL 컨테이너 이름: `spring-skillary-back-db-1`

3. **포트 충돌**: 
   - 백엔드: 8080 포트
   - 프론트엔드: 3000 포트
   - MySQL: 3306 포트

4. **프로필**: 백엔드는 `local` 프로필을 사용합니다 (`application-local.yml`)

## 🐛 문제 해결

### 백엔드가 시작되지 않는 경우
- MySQL이 실행 중인지 확인
- `application-local.yml`의 데이터베이스 설정 확인
- 이메일 설정이 올바른지 확인

### 프론트엔드가 시작되지 않는 경우
- `npm install`이 완료되었는지 확인
- 포트 3000이 사용 중인지 확인: `lsof -ti:3000`

### 데이터베이스 연결 오류
- MySQL 컨테이너 상태 확인: `docker ps | grep mysql`
- 데이터베이스 이름, 사용자명, 비밀번호 확인
